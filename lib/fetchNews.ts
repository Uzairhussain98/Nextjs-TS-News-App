const fetchNews = async (
  category?: Category | string,
  keyword?: string,
  isDynamic?: Boolean
) => {
  //Query with graphql
  //fetch function with next13
  //Sort function with images or not images
  //return response
};

export default fetchNews;
